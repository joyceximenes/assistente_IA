## Agent skills

### Issue tracker

Issues live as local markdown files under `.scratch/` (fallback: `gh` CLI not installed yet — switch to GitHub Issues on `github.com/joyceximenes/assistente_IA` by re-running `/setup-matt-pocock-skills`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root (created lazily when needed). See `docs/agents/domain.md`.
